{
  "name": "vite-environment-6.0.0-alpha-experimentations",
  "scripts": {
    "postinstall": "pnpm build:packages",
    "build:packages": "pnpm --filter vite-environment-provider-workerd --filter vite-environment-provider-node-vm --sequential build",
    "prettify": "prettier --ignore-path .gitignore --write ."
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "pnpm": {
    "overrides": {
      "workerd": "1.20240524.0"
    },
    "patchedDependencies": {
      "vite@6.0.0-alpha.11": "patches/vite@6.0.0-alpha.11.patch"
    }
  }
}
